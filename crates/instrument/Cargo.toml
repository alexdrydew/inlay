[package]
name = "inlay-instrument"
version = "0.1.0"
edition = "2024"
license = "MIT"
include = ["/Cargo.toml", "/src/**/*.rs"]

[dependencies]
inlay-instrument-macros = { path = "../instrument-macros" }
