[package]
name = "inlay-instrument-macros"
version = "0.1.0"
edition = "2024"
license = "MIT"
include = ["/Cargo.toml", "/src/**/*.rs"]

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
